interface:
  display_name: "Apple Schedule"
  short_description: "Manage Apple Calendar events and schedules"
  default_prompt: "Use $apple-schedule to review and manage my Apple Calendar schedule."

policy:
  allow_implicit_invocation: false
